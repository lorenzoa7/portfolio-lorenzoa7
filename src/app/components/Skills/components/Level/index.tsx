import { LevelItems } from './LevelItems'
import LevelRoot from './LevelRoot'
import LevelTitle from './LevelTitle'

export const Level = {
  Root: LevelRoot,
  Title: LevelTitle,
  Items: LevelItems,
}
